import styled from 'styled-components';
import { InformationDefault } from '../../config/colors';

export const Price = styled.p`
  color: ${InformationDefault};
  font-size: 35px;
  font-weight: bold;
`;
